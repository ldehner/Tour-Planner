<Window x:Class="Tour_planner.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tour_planner"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="650" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">
            <ToolBar Style="{StaticResource MaterialDesignToolBar}" ClipToBounds="False">
                <Menu>
                    <MenuItem Header="_File">
                        <MenuItem Header="New" />
                        <MenuItem Header="Open" />
                        <MenuItem Header="Save" Icon="{materialDesign:PackIcon Kind=ContentSave}" />
                        <MenuItem Header="Save As..." />
                    </MenuItem>
                </Menu>
                <Separator/>
                <Menu>
                    <MenuItem Header="Edit" />
                </Menu>
                <Separator/>
                <Menu>
                    <MenuItem Header="Options" Width="150">

                    </MenuItem>
                </Menu>
                <Separator/>
                <Menu>
                    <MenuItem Header="Help" Width="104" />
                </Menu>
                <Separator/>
                <TextBox x:Name="SearchTextBox" materialDesign:HintAssist.Hint="Search" Width="200" HorizontalAlignment="Right" />
                <Button Content="{materialDesign:PackIcon Kind=Search}"  Style="{StaticResource MaterialDesignPaperLightButton}"  />

            </ToolBar>
        </ToolBarTray>
        <ToolBar Grid.Column="0" Grid.Row="1">
            <Label Content="Touren" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center" />
            <Separator/>
            <Button Content="{materialDesign:PackIcon Kind=Add}" Margin="5" Click="OpenTourCreate"/>
            <Separator/>
            <Button Content="{materialDesign:PackIcon Kind=Minus}" Margin="5" Click="OpenTourDelete"/>
            <Separator/>

        </ToolBar>
         <ListView SelectedItem="{Binding TourModel}" Grid.Column="0" Grid.Row="2" Margin="5,0" Background="DarkGray" ItemsSource="{Binding TourList}">
            <ListView.ItemTemplate>
                <DataTemplate >
                    <Label  Grid.Column="0" HorizontalAlignment="Left" Content="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="2" Grid.Row="2">
            <DockPanel >
                <Label DockPanel.Dock="Left" Content="{Binding TourModel.Name, Mode=OneWay}" FontSize="24" FontWeight="Bold" ></Label>
            </DockPanel>

            <Label Content="{Binding TourModel.TravelRoute}" FontSize="14" ></Label>
            <Label Content="{Binding TourModel.DurationCalculated}" FontSize="14"></Label>
            <Label Content="{Binding TourModel.DistanceCalucalted}" FontSize="14"></Label>
            <Label IsEnabled="{Binding isSelected}" Content="Description:" FontSize="14" FontWeight="Bold"></Label>
            <Label Content="{Binding TourModel.Description}" FontSize="14"></Label>

            <Button IsEnabled="{Binding isSelected}" Content="Update Tour" Click="OpenChangeWindow"></Button> 
        </StackPanel>
        

    </Grid>
</Window>
